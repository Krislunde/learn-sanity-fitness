{
  "name": "learn-sanity-fitness",
  "private": true,
  "version": "1.0.0",
  "description": "Fitness content operations, built while following courses on sanity.io/learn",
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "build": "pnpm run --parallel build",
    "start": "pnpm run --parallel start",
    "lint": "pnpm run --recursive lint",
    "typecheck": "pnpm run --recursive typecheck"
  },
  "packageManager": "pnpm@11.20.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  },
  "devDependencies": {
    "@sanity/blueprints": "0.23.4",
    "@sanity/functions": "1.6.5"
  },
  "dependencies": {
    "@sanity/client": "^7.26.2"
  }
}
