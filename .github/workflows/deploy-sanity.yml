name: Deploy Sanity Studio

on:
  push:
    branches:
      - main
      - development
  pull_request:
    types: [opened, synchronize, reopened, closed]

permissions:
  contents: read
  pull-requests: write

defaults:
  run:
    working-directory: apps/studio

env:
  SANITY_AUTH_TOKEN: ${{ secrets.SANITY_AUTH_TOKEN }}
  # Base Studio hostname. Falls back to the value committed in apps/studio/.env
  # so a deploy still works if the repo variable has not been set.
  STUDIO_HOSTNAME: ${{ vars.HOSTNAME || 'fit' }}

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    environment:
      name: ${{ github.ref == 'refs/heads/main' && 'Production' || github.ref == 'refs/heads/development' && 'Development' || 'Preview' }}
      url: ${{ steps.deploy.outputs.STUDIO_URL }}
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          package_json_file: apps/studio/package.json
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: pnpm
          cache-dependency-path: apps/studio/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile

      - name: Set Studio hostname
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "SANITY_STUDIO_HOSTNAME=${STUDIO_HOSTNAME}-pr-${{ github.event.pull_request.number }}" >> "$GITHUB_ENV"
          elif [ "${{ github.ref_name }}" = "development" ]; then
            # Development gets its own hostname and dataset so it never
            # overwrites the production Studio.
            echo "SANITY_STUDIO_HOSTNAME=${STUDIO_HOSTNAME}-development" >> "$GITHUB_ENV"
            echo "SANITY_ACTIVE_ENV=development" >> "$GITHUB_ENV"
          else
            echo "SANITY_STUDIO_HOSTNAME=${STUDIO_HOSTNAME}" >> "$GITHUB_ENV"
          fi

      - name: Build and deploy Sanity Studio
        id: deploy
        run: |
          if [ -z "${SANITY_STUDIO_HOSTNAME}" ]; then
            echo "Error: SANITY_STUDIO_HOSTNAME is not set" >&2
            exit 1
          fi
          if [ "${{ github.ref_name }}" = "development" ]; then
            pnpm run deploy --source-maps --yes
          else
            pnpm run deploy --yes
          fi
          echo "STUDIO_URL=https://${SANITY_STUDIO_HOSTNAME}.sanity.studio" >> "$GITHUB_OUTPUT"

      - name: Post preview link
        if: github.event_name == 'pull_request' && github.event.action == 'opened'
        uses: actions/github-script@v7
        with:
          script: |
            const body = [
              '**🚀 Preview environment has been deployed!**',
              `Visit [${process.env.STUDIO_URL}](${process.env.STUDIO_URL}) to see your changes.`,
              "*This is a temporary environment that will be undeployed when this PR is merged or closed.*"
            ].join('\n\n')
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body,
            })
        env:
          STUDIO_URL: ${{ steps.deploy.outputs.STUDIO_URL }}

  teardown:
    name: Teardown
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    environment:
      name: Preview
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          package_json_file: apps/studio/package.json
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: pnpm
          cache-dependency-path: apps/studio/pnpm-lock.yaml
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Cleanup PR preview
        run: pnpm exec sanity undeploy --yes
        env:
          SANITY_STUDIO_HOSTNAME: ${{ vars.HOSTNAME || 'fit' }}-pr-${{ github.event.pull_request.number }}
